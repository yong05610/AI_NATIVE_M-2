# PRD: AI 학습시간 분석 비서

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | AI 학습시간 분석 비서 PRD |
| 버전 | v1.0 |
| 목적 | 웹서비스 개발을 위한 제품 요구사항 정의 |
| 프로젝트 유형 | 학습용 AI 웹서비스 |
| 주요 기술 | FastAPI, Firestore, OpenAI API, Vanilla JavaScript |
| 배포 목표 | Render 백엔드 배포, Vercel 프론트엔드 배포 |

---

## 2. 프로젝트 개요

### 2.1 프로젝트명

AI 학습시간 분석 비서

### 2.2 한 줄 설명

사용자가 매일 학습한 시간을 기록하면, 시계열 데이터를 기반으로 학습 흐름을 분석하고 AI가 맞춤형 학습 조언을 제공하는 웹서비스입니다.

### 2.3 프로젝트 목적

이 프로젝트의 목적은 다음과 같습니다.

1. 사용자가 날짜별 학습시간을 기록할 수 있게 한다.
2. 기록된 학습시간 데이터를 Firestore에 저장한다.
3. 저장된 데이터를 기반으로 기본 통계를 제공한다.
4. 사용자가 AI에게 질문하면 AI가 학습 기록을 참고하여 답변한다.
5. 프론트엔드에서 데이터 입력, 조회, 분석, AI 채팅을 사용할 수 있게 한다.
6. 완성된 서비스를 Render와 Vercel에 배포한다.

---

## 3. 문제 정의

학생은 자신의 공부량을 꾸준히 기록하기 어렵고, 기록하더라도 데이터를 해석하기 어렵습니다.

대표적인 문제는 다음과 같습니다.

- 최근 공부량이 늘고 있는지 줄고 있는지 알기 어렵다.
- 이번 주 공부 시간이 충분한지 판단하기 어렵다.
- 어떤 날에 공부를 많이 했는지 한눈에 보기 어렵다.
- 기록된 데이터를 바탕으로 구체적인 학습 조언을 받기 어렵다.
- 단순 기록 앱은 많지만, 기록 데이터를 AI가 해석해주는 기능은 부족하다.

이 서비스는 학습시간 기록, 통계 분석, AI 조언 기능을 하나의 웹서비스로 제공하여 위 문제를 해결합니다.

---

## 4. 목표 사용자

### 4.1 주요 사용자

- 자신의 학습 시간을 기록하고 싶은 학생
- 공부 습관을 만들고 싶은 학습자
- 학습 패턴을 확인하고 싶은 사용자
- AI에게 학습 조언을 받고 싶은 사용자

### 4.2 사용자 시나리오

#### 시나리오 1. 학습시간 기록

사용자는 하루 공부가 끝난 뒤 다음 정보를 입력합니다.

- 날짜
- 학습시간
- 메모

```
    날짜: 2025-01-10
    학습시간: 120분
    메모: FastAPI 라우팅과 Pydantic 모델 학습

```

#### 시나리오 2. 학습시간 확인

사용자는 지금까지 입력한 학습 기록 목록을 확인합니다.

#### 시나리오 3. 학습 통계 확인

사용자는 전체 학습시간, 평균 학습시간, 최대 학습시간, 최근 흐름을 확인합니다.

#### 시나리오 4. AI에게 질문

사용자는 AI에게 다음과 같이 질문합니다.

    최근 학습 흐름이 어때?
    이번 주 공부량은 괜찮은 편이야?
    다음 주 학습 계획을 추천해줘.

AI는 저장된 학습 데이터를 참고하여 답변합니다.

---

## 5. 핵심 기능 요구사항

## 5.1 학습시간 기록 등록 기능

### 설명

사용자는 날짜별 학습시간과 메모를 입력하여 저장할 수 있습니다.

### 입력값

| 필드 | 타입 | 필수 여부 | 설명 |
|---|---|---|---|
| date | string | 필수 | 학습 날짜. 형식은 YYYY-MM-DD |
| value | number | 필수 | 학습 시간. 단위는 분 |
| memo | string | 선택 | 학습 내용 메모 |

### 예시 데이터

```
    {
      "date": "2025-01-10",
      "value": 120,
      "memo": "FastAPI 기초 학습"
    }
```

### 요구사항

- 사용자는 날짜를 입력할 수 있어야 한다.
- 사용자는 학습시간을 분 단위로 입력할 수 있어야 한다.
- 사용자는 학습 메모를 입력할 수 있어야 한다.
- 입력된 데이터는 Firestore에 저장되어야 한다.
- 학습시간은 0보다 큰 숫자여야 한다.
- 날짜는 비어 있으면 안 된다.

---

## 5.2 학습시간 기록 조회 기능

### 설명

사용자는 저장된 학습시간 기록 목록을 확인할 수 있습니다.

### 요구사항

- 저장된 학습 기록을 목록으로 조회할 수 있어야 한다.
- 각 기록에는 날짜, 학습시간, 메모가 표시되어야 한다.
- 가능하면 날짜 기준 내림차순으로 표시한다.
- 데이터가 없을 경우 빈 목록 또는 안내 메시지를 반환한다.

### 응답 예시
```
    [
      {
        "id": "abc123",
        "date": "2025-01-10",
        "value": 120,
        "memo": "FastAPI 기초 학습"
      },
      {
        "id": "def456",
        "date": "2025-01-09",
        "value": 90,
        "memo": "JavaScript fetch 복습"
      }
    ]
```
---

## 5.3 학습시간 기록 수정 기능

### 설명

사용자는 기존 학습시간 기록의 날짜, 학습시간, 메모를 수정할 수 있습니다.

### 요구사항

- 특정 학습 기록을 ID 기준으로 수정할 수 있어야 한다.
- 수정된 데이터는 Firestore에 반영되어야 한다.
- 수정 후 목록 조회 시 변경된 데이터가 표시되어야 한다.
- 존재하지 않는 ID 수정 요청 시 적절한 오류 메시지를 반환한다.

---

## 5.4 학습시간 기록 삭제 기능

### 설명

사용자는 잘못 입력한 학습시간 기록을 삭제할 수 있습니다.

### 요구사항

- 특정 학습 기록을 ID 기준으로 삭제할 수 있어야 한다.
- 삭제 후 목록 조회 시 해당 데이터가 보이지 않아야 한다.
- 존재하지 않는 ID 삭제 요청 시 적절한 오류 메시지를 반환한다.

---

## 5.5 학습시간 요약 분석 기능

### 설명

저장된 학습시간 데이터를 기반으로 기본 통계를 계산합니다.

### 제공할 통계

| 항목 | 설명 |
|---|---|
| count | 전체 기록 개수 |
| total_minutes | 전체 학습시간 합계 |
| average_minutes | 평균 학습시간 |
| max_minutes | 가장 긴 학습시간 |
| min_minutes | 가장 짧은 학습시간 |
| recent_7_days_total | 최근 7일 학습시간 합계 |
| recent_trend | 최근 학습 흐름 |

### recent_trend 기준

recent_trend는 간단한 규칙 기반으로 계산합니다.

- 최근 데이터가 이전보다 증가하면 increasing
- 최근 데이터가 이전보다 감소하면 decreasing
- 변화가 크지 않으면 stable
- 데이터가 부족하면 not_enough_data

구현 방식은 개발 과정에서 단순하게 시작해도 됩니다.

### 응답 예시
```
    {
      "count": 10,
      "total_minutes": 900,
      "average_minutes": 90,
      "max_minutes": 180,
      "min_minutes": 30,
      "recent_7_days_total": 520,
      "recent_trend": "increasing"
    }
```
### 요구사항

- 데이터가 없을 때도 서버 오류가 발생하면 안 된다.
- 데이터가 없을 경우 count는 0으로 반환한다.
- 평균 계산 시 0으로 나누는 오류가 없어야 한다.
- 날짜 기반 계산이 가능해야 한다.
- 초기 시계열 데이터는 최소 100개 이상의 데이터 포인트를 준비해야 한다.

---

## 5.6 AI 학습 비서 채팅 기능

### 설명

사용자가 질문을 입력하면, AI가 저장된 학습시간 데이터를 참고하여 답변합니다.

### 사용자 질문 예시

    최근 학습 흐름이 어때?
    공부 시간이 부족한 날이 많아?
    다음 주 학습 계획을 추천해줘.
    내 학습 습관의 장점과 약점을 알려줘.

### 요구사항

- 사용자는 질문 메시지를 입력할 수 있어야 한다.
- 백엔드는 Firestore에서 학습시간 데이터를 조회해야 한다.
- 백엔드는 학습시간 데이터 요약을 생성하여 AI 프롬프트에 포함해야 한다.
- OpenAI API를 호출하여 답변을 생성해야 한다.
- AI 답변은 사용자에게 반환되어야 한다.
- AI는 학습 코치처럼 친절하고 구체적으로 답변해야 한다.
- 학습 데이터가 부족한 경우, 데이터가 부족하다는 점을 설명하고 일반적인 조언을 제공해야 한다.

### AI 답변 스타일

- 친절한 학습 코치 말투
- 너무 길지 않게 핵심 위주
- 사용자의 학습 데이터를 근거로 설명
- 다음 행동을 제안
- 비난하거나 과도하게 단정하지 않음

### 요청 예시

    {
      "message": "최근 학습 흐름이 어때?"
    }

### 응답 예시

    {
      "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다. 특히 최근 7일 동안 꾸준히 기록이 이어진 점이 좋습니다. 다음 주에는 하루 최소 90분을 목표로 유지해보면 좋겠습니다."
    }

---

## 5.7 AI 대화 기록 저장 기능

### 설명

사용자의 질문과 AI 답변을 Firestore에 저장합니다.

### 저장 데이터 예시

    {
      "message": "최근 학습 흐름이 어때?",
      "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다.",
      "created_at": "2025-01-10T12:30:00Z"
    }

### 요구사항

- 사용자의 질문과 AI 답변을 함께 저장한다.
- 생성 시간을 저장한다.
- 저장된 대화 기록을 조회할 수 있어야 한다.
- 특정 대화 기록을 불러오고 삭제할 수 있어야 한다.
- 최신 대화가 위에 오도록 정렬하는 것을 권장한다.

---

## 5.8 프론트엔드 기능

### 설명

사용자는 브라우저에서 학습시간 기록, 통계 확인, AI 채팅을 사용할 수 있어야 합니다.

### 화면 구성

단일 페이지 웹앱으로 구현합니다.

필수 영역:

1. 서비스 제목 영역
2. 학습시간 입력 영역
3. 학습기록 목록 영역
4. 학습 통계 요약 영역
5. AI 질문 입력 영역
6. AI 답변 표시 영역
7. 대화 기록 영역

### 요구사항

- 사용자는 날짜를 입력할 수 있어야 한다.
- 사용자는 학습시간을 입력할 수 있어야 한다.
- 사용자는 메모를 입력할 수 있어야 한다.
- 등록 버튼을 누르면 백엔드 API로 데이터가 전송되어야 한다.
- 등록 후 학습기록 목록과 요약 통계가 갱신되어야 한다.
- 사용자는 저장된 학습시간 데이터를 화면에서 수정할 수 있어야 한다.
- 사용자는 AI에게 질문할 수 있어야 한다.
- AI 답변이 화면에 표시되어야 한다.
- 저장된 대화 기록을 화면에서 확인할 수 있어야 한다.
- 이전 대화를 선택하면 해당 대화 내용을 채팅창에 다시 표시해야 한다.
- API 호출 실패 시 간단한 오류 메시지를 보여준다.

---

## 6. 비기능 요구사항

## 6.1 사용성

- UI는 복잡하지 않고 직관적이어야 한다.
- 학습용 프로젝트이므로 기능이 명확하게 드러나야 한다.
- 모바일보다는 데스크톱 브라우저 기준으로 우선 구현한다.

## 6.2 안정성

- 데이터가 없을 때도 오류 없이 동작해야 한다.
- API 오류 발생 시 프론트엔드에서 사용자에게 안내해야 한다.
- 환경변수가 없을 경우 서버에서 명확한 오류 메시지를 제공해야 한다.

## 6.3 보안

- OpenAI API Key는 프론트엔드에 노출되면 안 된다.
- Firebase 서비스 계정 정보는 GitHub에 올리면 안 된다.
- 민감정보는 .env 또는 Render 환경변수로 관리한다.
- .env 파일은 .gitignore에 포함한다.

## 6.4 확장성

추후 다음 기능을 추가할 수 있도록 구조를 단순하고 명확하게 유지한다.

- 사용자 로그인
- 과목별 학습시간 분류
- 그래프 시각화
- 월별/주별 리포트
- 목표 학습시간 설정
- AI 주간 리포트 생성

---

## 7. 데이터 설계

## 7.1 Firestore 컬렉션

### 7.1.1 data 컬렉션

학습시간 기록을 저장합니다.

문서 구조:

    {
      "date": "2025-01-10",
      "value": 120,
      "memo": "FastAPI 기초 학습",
      "created_at": "2025-01-10T12:00:00Z"
    }

필드 설명:

| 필드 | 타입 | 필수 여부 | 설명 |
|---|---|---|---|
| date | string | 필수 | 학습 날짜 |
| value | number | 필수 | 학습시간. 단위는 분 |
| memo | string | 선택 | 학습 내용 메모 |
| created_at | string 또는 timestamp | 권장 | 데이터 생성 시간 |

### 7.1.2 conversations 컬렉션

AI 채팅 기록을 저장합니다.

문서 구조:

    {
      "message": "최근 학습 흐름이 어때?",
      "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다.",
      "created_at": "2025-01-10T12:30:00Z"
    }

필드 설명:

| 필드 | 타입 | 필수 여부 | 설명 |
|---|---|---|---|
| message | string | 필수 | 사용자 질문 |
| answer | string | 필수 | AI 답변 |
| created_at | string 또는 timestamp | 권장 | 대화 생성 시간 |

---

## 8. API 요구사항

## 8.1 기본 상태 확인 API

GET /

서버 상태를 확인합니다.

응답 예시:

    {
      "message": "AI Study Time Assistant API"
    }

---

## 8.2 Firestore 연결 확인 API

GET /api/health/firestore

Firestore 연결 상태를 확인합니다.

응답 예시:

    {
      "status": "ok",
      "message": "Firestore connected"
    }

---

## 8.3 학습시간 등록 API

POST /api/data

학습시간 데이터를 등록합니다.

요청 예시:

    {
      "date": "2025-01-10",
      "value": 120,
      "memo": "FastAPI 기초 학습"
    }

응답 예시:

    {
      "id": "abc123",
      "date": "2025-01-10",
      "value": 120,
      "memo": "FastAPI 기초 학습"
    }

---

## 8.4 학습시간 목록 조회 API

GET /api/data

학습시간 데이터 목록을 조회합니다.

응답 예시:

    [
      {
        "id": "abc123",
        "date": "2025-01-10",
        "value": 120,
        "memo": "FastAPI 기초 학습"
      }
    ]

---

## 8.5 학습시간 수정 API

PUT /api/data/{id}

특정 학습시간 데이터의 날짜, 학습시간, 메모를 수정합니다.

요청 예시:

    {
      "date": "2025-01-10",
      "value": 150,
      "memo": "FastAPI 라우팅 복습"
    }

응답 예시:

    {
      "id": "abc123",
      "date": "2025-01-10",
      "value": 150,
      "memo": "FastAPI 라우팅 복습"
    }

---

## 8.6 학습시간 삭제 API

DELETE /api/data/{id}

특정 학습시간 데이터를 삭제합니다.

응답 예시:

    {
      "message": "Data deleted successfully"
    }

---

## 8.7 학습시간 요약 API

GET /api/data/summary

학습시간 데이터 요약 정보를 반환합니다.

응답 예시:

    {
      "count": 10,
      "total_minutes": 900,
      "average_minutes": 90,
      "max_minutes": 180,
      "min_minutes": 30,
      "recent_7_days_total": 520,
      "recent_trend": "increasing"
    }

---

## 8.8 AI 채팅 API

POST /api/chat

사용자의 질문에 대해 AI 답변을 생성합니다.

요청 예시:

    {
      "message": "최근 학습 흐름이 어때?"
    }

응답 예시:

    {
      "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다. 다음 주에는 하루 최소 90분을 목표로 유지해보면 좋겠습니다."
    }

---

## 8.9 AI 대화 기록 저장 API

POST /api/conversations

사용자 질문과 AI 답변을 대화 기록으로 저장합니다. POST /api/chat으로 생성된 대화도 같은 형식으로 자동 저장합니다.

요청 예시:

    {
      "message": "최근 학습 흐름이 어때?",
      "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다."
    }

---

## 8.10 AI 대화 기록 목록 조회 API

GET /api/conversations

저장된 AI 대화 기록을 조회합니다.

응답 예시:

    [
      {
        "id": "chat123",
        "message": "최근 학습 흐름이 어때?",
        "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다.",
        "created_at": "2025-01-10T12:30:00Z"
      }
    ]

---

## 8.11 AI 대화 기록 상세 조회 API

GET /api/conversations/{id}

특정 대화의 사용자 질문과 AI 답변을 조회하여 대화 불러오기에 사용합니다.

응답 예시:

    {
      "id": "chat123",
      "message": "최근 학습 흐름이 어때?",
      "answer": "최근 기록을 보면 학습 시간이 조금씩 증가하는 흐름입니다.",
      "created_at": "2025-01-10T12:30:00Z"
    }

---

## 8.12 AI 대화 기록 삭제 API

DELETE /api/conversations/{id}

특정 AI 대화 기록을 삭제합니다.

응답 예시:

    {
      "message": "Conversation deleted successfully"
    }

---

## 9. 기술 스택

## 9.1 백엔드

| 항목 | 기술 |
|---|---|
| 언어 | Python 3.11 이상 권장, Python 3.12 로컬 검증, Python 3.14 검증 제외 |
| 웹 프레임워크 | FastAPI |
| 실행 서버 | Uvicorn |
| 데이터 검증 | Pydantic |
| 환경변수 관리 | python-dotenv |
| 패키지 관리 | python -m pip, requirements.txt |
| 가상환경 | 프로젝트 루트 .venv |

---

## 9.2 프론트엔드

| 항목 | 기술 |
|---|---|
| 언어 | HTML, CSS, JavaScript |
| 방식 | Vanilla JavaScript |
| API 호출 | fetch |
| 화면 구성 | 단일 페이지 웹앱 |

---

## 9.3 데이터베이스

| 항목 | 기술 |
|---|---|
| 데이터베이스 | Firebase Firestore |
| 주요 컬렉션 | data, conversations |
| 사용 목적 | 학습시간 기록 저장, AI 대화 기록 저장 |

---

## 9.4 AI

| 항목 | 기술 |
|---|---|
| AI API | OpenAI API |
| 사용 목적 | 학습 기록 기반 질의응답 |
| API Key 관리 | 백엔드 환경변수 |

---

## 9.5 배포

| 항목 | 기술 |
|---|---|
| 백엔드 배포 | Render |
| 프론트엔드 배포 | Vercel |
| 코드 저장소 | GitHub |

---

## 10. 환경변수 요구사항

백엔드는 환경변수를 사용하여 민감정보를 관리합니다.

### 10.1 필수 환경변수

    OPENAI_API_KEY=your_openai_api_key
    FIREBASE_PROJECT_ID=your_firebase_project_id
    FIREBASE_CLIENT_EMAIL=your_firebase_client_email
    FIREBASE_PRIVATE_KEY=your_firebase_private_key

### 10.2 선택 환경변수

    FRONTEND_ORIGIN=http://localhost:5500

배포 후 예시:

    FRONTEND_ORIGIN=https://your-frontend.vercel.app

### 10.3 환경변수 관리 원칙

- .env 파일은 로컬 개발용으로만 사용한다.
- .env 파일은 GitHub에 업로드하지 않는다.
- .env.example 파일에는 실제 키가 아닌 예시 값만 작성한다.
- Render 배포 시에는 Render Dashboard에서 환경변수를 설정한다.
- OpenAI API Key는 절대 프론트엔드 코드에 작성하지 않는다.
- Firebase 서비스 계정 정보도 GitHub에 올리지 않는다.

---

## 11. 화면 요구사항

## 11.1 전체 화면 구성

프론트엔드는 단일 페이지로 구성합니다.

화면에는 다음 영역이 포함되어야 합니다.

1. 헤더 영역
2. 학습시간 입력 영역
3. 학습 통계 요약 영역
4. 학습기록 목록 영역
5. AI 질문 입력 영역
6. AI 답변 영역
7. AI 대화 기록 영역

---

## 11.2 헤더 영역

### 표시 내용

- 서비스명: AI 학습시간 분석 비서
- 간단한 설명 문구

예시:

    매일의 학습시간을 기록하고 AI에게 학습 조언을 받아보세요.

---

## 11.3 학습시간 입력 영역

### 입력 요소

| 요소 | 설명 |
|---|---|
| 날짜 입력 | 학습 날짜 선택 |
| 학습시간 입력 | 분 단위 숫자 입력 |
| 메모 입력 | 학습 내용 입력 |
| 등록 버튼 | 입력한 데이터를 저장 |

### 동작

- 등록 버튼 클릭 시 POST /api/data 호출
- 등록 성공 시 입력 폼 초기화
- 등록 성공 시 학습기록 목록 갱신
- 등록 성공 시 요약 통계 갱신
- 실패 시 오류 메시지 표시

---

## 11.4 학습 통계 요약 영역

### 표시 항목

- 전체 기록 개수
- 총 학습시간
- 평균 학습시간
- 최대 학습시간
- 최소 학습시간
- 최근 7일 학습시간
- 최근 학습 흐름

### 동작

- 페이지 로드 시 GET /api/data/summary 호출
- 학습시간 등록 또는 삭제 후 다시 호출하여 갱신

---

## 11.5 학습기록 목록 영역

### 표시 항목

각 학습기록은 다음 정보를 표시합니다.

- 날짜
- 학습시간
- 메모
- 수정 버튼
- 삭제 버튼

### 동작

- 페이지 로드 시 GET /api/data 호출
- 수정 버튼 클릭 시 기존 날짜, 학습시간, 메모를 입력 폼에 채움
- 사용자가 값을 변경하고 저장하면 PUT /api/data/{id} 호출
- 수정 성공 시 목록과 요약 통계 갱신
- 삭제 버튼 클릭 시 DELETE /api/data/{id} 호출
- 삭제 성공 시 목록과 요약 통계 갱신

---

## 11.6 AI 질문 입력 영역

### 입력 요소

| 요소 | 설명 |
|---|---|
| 질문 입력창 | AI에게 물어볼 질문 입력 |
| 질문 버튼 | 질문 전송 |

### 동작

- 질문 버튼 클릭 시 POST /api/chat 호출
- AI 응답을 화면에 표시
- 질문과 답변은 Firestore에 저장
- 응답 후 대화 기록 영역 갱신

---

## 11.7 AI 답변 영역

### 표시 내용

- 사용자가 방금 보낸 질문
- AI가 생성한 답변
- 로딩 상태 메시지

### 예시

    질문: 최근 학습 흐름이 어때?

    AI 답변:
    최근 기록을 보면 학습 시간이 점점 늘어나는 흐름입니다.
    특히 최근 7일 동안 꾸준히 공부한 점이 좋습니다.
    다음 주에는 하루 90분 이상을 목표로 해보세요.

---

## 11.8 AI 대화 기록 영역

### 표시 항목

- 사용자 질문
- AI 답변
- 생성 시간

### 동작

- 페이지 로드 시 GET /api/conversations 호출
- 이전 대화 클릭 시 GET /api/conversations/{id}를 호출하여 해당 질문과 답변을 채팅창에 복원해 다시 표시
- 새로운 질문 후 대화 기록 갱신
- 최신 대화가 위에 오도록 표시하는 것을 권장

---

## 12. 검증 기준

## 12.1 백엔드 검증 기준

다음 항목이 정상 동작해야 합니다.

- FastAPI 서버가 실행된다.
- / 요청에 정상 응답한다.
- /docs Swagger 문서에 접속할 수 있다.
- Firestore 연결 확인 API가 정상 응답한다.
- 학습시간 데이터를 등록할 수 있다.
- 학습시간 데이터를 조회할 수 있다.
- 학습시간 데이터를 수정할 수 있다.
- 학습시간 데이터를 삭제할 수 있다.
- 학습시간 요약 API가 정상 응답한다.
- OpenAI API를 통해 AI 답변을 받을 수 있다.
- AI 대화 기록이 Firestore에 저장된다.
- AI 대화 기록을 조회할 수 있다.
- AI 대화 기록을 삭제할 수 있다.
- 특정 AI 대화 기록을 불러올 수 있다.

---

## 12.2 프론트엔드 검증 기준

다음 항목이 정상 동작해야 합니다.

- 브라우저에서 index.html 화면이 열린다.
- 날짜, 학습시간, 메모를 입력할 수 있다.
- 등록 버튼을 누르면 학습시간 데이터가 저장된다.
- 저장된 학습기록이 화면에 표시된다.
- 수정 버튼을 누르면 기존 값이 입력 폼에 표시되고, 변경값 저장 후 목록이 갱신된다.
- 삭제 버튼으로 학습기록을 삭제할 수 있다.
- 요약 통계가 화면에 표시된다.
- AI 질문을 입력하고 답변을 받을 수 있다.
- AI 대화 기록이 화면에 표시된다.
- 이전 대화를 클릭하면 해당 질문과 답변이 채팅창에 다시 표시된다.
- API 오류 발생 시 사용자가 알 수 있는 메시지가 표시된다.

---

## 12.3 데이터 검증 기준

Firestore에서 다음 내용을 확인할 수 있어야 합니다.

### data 컬렉션

- 초기 시계열 데이터가 최소 100개 이상 준비되어 있다.
- 학습 날짜가 저장되어 있다.
- 학습시간이 숫자로 저장되어 있다.
- 메모가 저장되어 있다.
- 생성 시간이 저장되어 있다.

### conversations 컬렉션

- 사용자 질문이 저장되어 있다.
- AI 답변이 저장되어 있다.
- 생성 시간이 저장되어 있다.

---

## 12.4 AI 기능 검증 기준

AI 기능은 다음 기준을 만족해야 합니다.

- 사용자의 질문을 OpenAI API로 전달한다.
- 저장된 학습시간 데이터를 AI 프롬프트에 포함한다.
- AI 답변이 학습 기록과 관련된 내용을 포함한다.
- 데이터가 부족할 경우 데이터 부족 상황을 설명한다.
- 답변은 학습 코치처럼 친절하고 구체적이어야 한다.

---

## 12.5 배포 검증 기준

### Render 백엔드 배포

다음 항목을 확인합니다.

- Render에 백엔드 서비스가 배포된다.
- Render 환경변수가 설정된다.
- 배포된 API 주소에서 / 요청이 정상 응답한다.
- 배포된 API 주소에서 /docs 접속이 가능하다.
- 배포된 API에서 Firestore와 OpenAI 기능이 동작한다.

### Vercel 프론트엔드 배포

다음 항목을 확인합니다.

- Vercel에 프론트엔드가 배포된다.
- 프론트엔드가 Render 백엔드 API를 호출한다.
- CORS 오류가 발생하지 않는다.
- 배포된 프론트엔드에서 데이터 등록, 조회, 요약, AI 채팅이 동작한다.

### README 및 제출 자료

다음 항목을 확인합니다.

- README에 서비스 소개, 기술 스택, 배포 URL, 로컬 실행 방법, 환경 변수 목록이 포함된다.
- README에 데이터 요약과 질문·답변이 보이는 채팅 화면 스크린샷이 포함된다.
- README에 CRUD 중 1개 동작이 보이는 데이터 관리 화면 스크린샷이 포함된다.
- README에 불러오기 동작이 보이는 대화 기록 화면 스크린샷이 포함된다.

---

## 13. 개발 우선순위

기능은 다음 순서로 개발합니다.

1. 프로젝트 기본 구조 생성
2. FastAPI 기본 서버 구성
3. 환경변수 구조 설정
4. Firestore 연결
5. 학습시간 데이터 CRUD API 구현
6. 학습시간 요약 API 구현
7. OpenAI 채팅 API 구현
8. AI 대화 기록 API 구현
9. 최소 100개 이상의 샘플 시계열 데이터 생성
10. 프론트엔드 구현
11. 로컬 통합 테스트
12. Render 백엔드 배포
13. Vercel 프론트엔드 배포
14. README 작성 및 최종 제출 정리

---

## 14. 제외 범위

이번 버전에서는 다음 기능은 구현하지 않습니다.

- 사용자 로그인
- 회원가입
- 사용자별 데이터 분리
- 과목별 학습시간 분류
- 그래프 라이브러리를 사용한 시각화
- 목표 학습시간 설정
- 알림 기능
- 모바일 앱
- 결제 기능

단, 추후 확장 가능성을 고려하여 코드 구조를 너무 복잡하게 만들지 않고 명확하게 유지합니다.

---

## 15. 성공 기준

이 프로젝트는 다음 조건을 만족하면 성공으로 판단합니다.

1. 사용자가 학습시간 데이터를 등록할 수 있다.
2. 등록된 데이터가 Firestore에 저장된다.
3. 사용자가 저장된 학습시간 목록을 볼 수 있다.
4. 사용자가 학습시간 데이터를 수정할 수 있다.
5. 사용자가 학습시간 데이터를 삭제할 수 있다.
6. 최소 100개 이상의 시계열 데이터가 준비되어 있다.
7. 학습시간 요약 통계를 확인할 수 있다.
8. 사용자가 AI에게 학습 관련 질문을 할 수 있다.
9. AI가 저장된 학습 데이터를 참고해 답변한다.
10. AI 대화 기록이 저장·조회·삭제되고, 선택한 대화를 불러올 수 있다.
11. 프론트엔드에서 모든 핵심 기능을 사용할 수 있다.
12. 백엔드는 Render에 배포된다.
13. 프론트엔드는 Vercel에 배포된다.
14. README에 실행 방법, 배포 주소, 필수 제출 스크린샷 3종이 정리된다.

---

## 16. Codex 전달용 요약

### 프로젝트 목표

AI 학습시간 분석 비서를 만든다.

사용자는 날짜별 학습시간과 메모를 입력하고, 저장된 학습시간 데이터를 기반으로 통계 요약과 AI 학습 조언을 받을 수 있다.

### 핵심 기능

- 학습시간 기록 등록
- 학습시간 기록 조회
- 학습시간 기록 수정
- 학습시간 기록 삭제
- 학습시간 요약 통계
- OpenAI 기반 AI 학습 비서 채팅
- AI 대화 기록 저장, 조회, 불러오기 및 삭제
- HTML/CSS/Vanilla JS 프론트엔드

### 기술 스택

- Backend: Python 3.11 이상 권장(Python 3.12 검증, Python 3.14 제외), FastAPI, Uvicorn, Pydantic
- Database: Firebase Firestore
- AI: OpenAI API
- Frontend: HTML, CSS, Vanilla JavaScript
- Deploy: Render, Vercel
- Environment: 프로젝트 루트 .venv, python -m pip, requirements.txt

### 주요 API

- GET /api/data
- POST /api/data
- PUT /api/data/{id}
- DELETE /api/data/{id}
- GET /api/data/summary
- POST /api/chat
- POST /api/conversations
- GET /api/conversations
- GET /api/conversations/{id}
- DELETE /api/conversations/{id}

### 데이터 모델

학습시간 데이터:

    {
      "date": "2025-01-10",
      "value": 120,
      "memo": "FastAPI 기초 학습",
      "created_at": "2025-01-10T12:00:00Z"
    }

AI 대화 기록:

    {
      "message": "최근 학습 흐름이 어때?",
      "answer": "최근 기록을 보면 학습 시간이 증가하는 흐름입니다.",
      "created_at": "2025-01-10T12:30:00Z"
    }

### 개발 원칙

- 한 번에 모든 기능을 만들지 말고 단계별로 구현한다.
- 각 단계가 끝날 때 실행 및 검증한다.
- 민감정보는 .env 또는 배포 환경변수로 관리한다.
- OpenAI API Key와 Firebase 인증정보는 프론트엔드에 노출하지 않는다.
- 데이터가 없을 때도 오류 없이 동작해야 한다.
- 코드 구조는 학습자가 이해할 수 있도록 단순하고 명확하게 작성한다.

